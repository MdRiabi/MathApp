import { AnswerStyleDirective } from './answer-style.directive';

describe('AnswerStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new AnswerStyleDirective();
    expect(directive).toBeTruthy();
  });
});
